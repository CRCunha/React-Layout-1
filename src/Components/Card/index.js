import React from 'react';
import './card.css';

const Card = () => (
    <div class="card">
        <div class="card-header">
            Card-Title
        </div>
    </div>
);

export default Card;